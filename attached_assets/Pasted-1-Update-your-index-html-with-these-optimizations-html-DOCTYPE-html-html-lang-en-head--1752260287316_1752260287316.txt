1. Update your index.html with these optimizations:
html<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <!-- Preconnect to external domains -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <!-- Critical CSS inline (add your above-the-fold styles here) -->
    <style>
      /* Add critical CSS here - just enough to render above-the-fold content */
      body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; }
      .loading { min-height: 100vh; display: flex; align-items: center; justify-content: center; }
    </style>
    
    <!-- Load fonts asynchronously -->
    <link rel="preload" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Lato:wght@300;400;500;600&family=Dancing+Script:wght@400;500;600;700&display=swap" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript><link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Lato:wght@300;400;500;600&family=Dancing+Script:wght@400;500;600;700&display=swap"></noscript>
    
    <!-- Other meta tags... -->
    
    <!-- Defer non-critical scripts -->
    <script type="module" src="/src/main.tsx" defer></script>
  </head>
  <body>
    <div id="root">
      <div class="loading">Loading...</div>
    </div>
  </body>
</html>
2. Optimize your Vite build for code splitting:
Update vite.config.ts:
typescriptimport { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import viteCompression from 'vite-plugin-compression';

export default defineConfig({
  plugins: [
    react(),
    viteCompression({
      algorithm: 'gzip',
      verbose: true,
      threshold: 1024,
      deleteOriginFile: false
    })
  ],
  build: {
    minify: true,
    // Enable code splitting
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          router: ['wouter']
        }
      }
    }
  }
});
3. Lazy load heavy components:
tsximport { lazy, Suspense } from 'react';

// Lazy load gallery
const Gallery = lazy(() => import('./components/Gallery'));

// In your component
<Suspense fallback={<div>Loading gallery...</div>}>
  <Gallery />
</Suspense>
4. Add this font loading script to prevent FOIT (Flash of Invisible Text):
html<script>
  // Font loading optimization
  if ('fonts' in document) {
    document.fonts.load('400 1em Playfair Display');
    document.fonts.load('400 1em Lato');
  }
</script>
These changes will:

Load fonts without blocking
Inline critical CSS
Defer JavaScript execution
Split code into smaller chunks
Improve your Largest Contentful Paint (LCP) score